__pycache__/
*.pyc
.DS_Store
courseware/build/.infographic_cache/

# Distribution archives (rebuildable — never pushed to GitHub)
*.zip

# Source material the project was derived from — not ours to republish.
**/reference/
**/Reference/
/PPT References/
!.claude/skills/**/reference/
!.claude/skills/**/reference/**

# Superseded courseware versions (kept locally only)
/courseware/archive/

# CONFIDENTIAL — assessment papers and answer keys are trainer-only and are
# distributed via Google Drive, never GitHub. Do not remove this rule.
/assessment/

# Internal Drive links + any local secrets (written by gdrive_push.py).
.env
.env.*
!.env.example

# Office lock files
~$*
